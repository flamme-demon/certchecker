<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Trust only system CAs by default (Android's trust store) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
